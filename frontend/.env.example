# Frontend environment variables
# Copy this to .env.local for local overrides (never commit .env.local)

# URL of the deployed backend API (required in production / Vercel)
# Example: https://your-backend.onrender.com/api
VITE_API_URL=http://localhost:8000/api
